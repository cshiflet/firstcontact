build/
build-*/
out/
Testing/
.cache/
AppDir/
*.AppImage
.vscode/
.idea/
*.user
*.user.*
.DS_Store

# CMake — note we keep the hand-written ./Makefile (top-level dev wrapper);
# CMake-generated Makefiles only live inside build directories, which the
# build/ build-*/ rules above already exclude.
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
compile_commands.json

# vcpkg
vcpkg_installed/

# Qt
*.qm
*.pro.user
moc_*.cpp
moc_*.h
qrc_*.cpp
ui_*.h
